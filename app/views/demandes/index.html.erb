
<br>
<div class="container" align="center">  
  <h1>  Demandes formulées par nos clients</h1>
  <p> Si le formulaire de recherche ne vous permet pas de trouver votre bonheur sur ce site, n'hésitez pas à formuler une demande
    la plus précise possible aux centaines de courtiers avec lesquels nous collaborons. Et n'oubliez surtout pas d'indiquer votre 
    numéro de téléphone ainsi que la somme dont vous disposez <i class="fa fa-smile-o" aria-hidden="true"></i>

  </p>
      <%= simple_form_for(Demande.new) do |f| %>
        <%= f.error_notification %>
            <div class="form-group has-feedback col-md-2">
              <div class="icon"><i class="pe-7s-pen"></i></div>
            </div>
            <div class="form-group has-feedback col-md-10">
              <div class="form-inputs">
                <%= f.text_area :description, label:false, class:'form-control', placeholder:'Bonjour tout le monde, je m\'appelle Mamadou. Je cherche ...'  %>
              </div>
            </div>
        
        
        <br>
        <div class="row">
            <div class="form-actions">
              <%= f.button :submit, "Formulez votre demande", class:'btn btn-success' %>
            </div>
        </div>
        
      <% end %>
</div>
<br>
<hr>  

<div class="container">
  <% @demandes.each do |demande| %>
    <div class="container">
        <h5 class="posted"><i class="fa fa-clock-o"></i> <%= demande.created_at.strftime("%d %b. %Y") +" "+ demande.created_at.strftime("%H:%M")  %></h5>
        <blockquote>  
          <p class="post__intro"> <%= demande.description %></p>
        </blockquote>
    </div>
    <hr>
  <% end %>
</div>